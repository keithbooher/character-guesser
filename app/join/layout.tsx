export const metadata = {
  title: 'Join a Game',
  description: "Have a game code? Enter it here to join your partner's Character Guesser session.",
};

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
