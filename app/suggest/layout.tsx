export const metadata = {
  title: 'Suggest a Show, Movie, or Game',
  description: "Missing a title from Character Guesser? Suggest it and we'll add it to the database.",
};

export default function SuggestLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
