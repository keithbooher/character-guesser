export const metadata = {
  title: 'Start a New Game',
  description: 'Create a new Character Guesser session. Choose online or local mode, enter emails, and start swiping.',
};

export default function NewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
